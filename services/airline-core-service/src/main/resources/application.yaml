server:
  port: 5002

spring:
  application:
    name: airline-core-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
