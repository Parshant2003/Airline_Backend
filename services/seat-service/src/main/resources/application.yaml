
server:
  port: 5006

spring:
  application:
    name: seat-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_seat
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

