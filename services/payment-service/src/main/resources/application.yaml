server:
  port: 5001

spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_payment
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
