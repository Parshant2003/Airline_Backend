server:
  port: 5001
spring:
  application:
    name: booking-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_booking
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update