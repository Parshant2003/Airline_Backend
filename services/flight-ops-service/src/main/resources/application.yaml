server:
  port: 5005

spring:
  application:
    name: flight-ops-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_flight
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
